# PlatformIO build artefacts
.pio/
.pioenvs/
.pio/build/
.pio/libdeps/
.piolibdeps/
build/
# Compiled output (common embedded/Arduino formats)
*.elf
*.bin
*.hex
*.map
*.lst
*.o
*.obj
*.a
*.lib
# PlatformIO more targets / temporary files
*.pio
*.pio?*
*.d
*.tmp
# Arduino/PlatformIO test & coverage
coverage/
test_results/
test_output/
# IDEs and editors
.vscode/
.idea/
*.iml
*.sublime-project
*.sublime-workspace
# OS generated files
.DS_Store
Thumbs.db
desktop.ini
# Editor swap/backup files
*~
*.swp
*.swo
*.bak
*.orig
# Logs and temporary files
*.log
*.gz
*.zip
# Python / virtualenvs (if present in repo)
__pycache__/
*.pyc
venv/
.env/
# Node (if any tooling uses Node)
node_modules/
# Secrets and private keys (do NOT commit API keys, certificates)
# `.env` is used for local secrets. Keep it out of git but commit `.env.example`.
.env
.env.local
credentials.json
secrets.json
*.pem
*.key
*.crt
src/generated_secrets.h
# Misc local machine files
local.settings.json
.vs/
# Optional: ignore serial monitor capture files, if you create them locally
serial.log

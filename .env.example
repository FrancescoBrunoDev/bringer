# Copy this file to .env and fill with your real credentials.
# This file is safe to commit as an example and is ignored by git.

# WiFi STA (station) mode credentials
WIFI_SSID=your_wifi_ssid_here
WIFI_PASSWORD=your_wifi_password_here

# Fallback Access Point (AP) credentials used if STA connection fails
AP_SSID=EPaper-AP
AP_PASSWORD=epaper123

# Optional: fixed AP IP address (leave empty to use DHCP)
# AP_IP_ADDRESS=192.168.4.1
